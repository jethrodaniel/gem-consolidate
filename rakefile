require "bundler/gem_tasks"

task :default => %i[test install fib]

task :test do
  Dir.chdir("example") { sh "bundle exec bench test" }
  sh "bundle exec bench test"
end

task :fib do
  sh "gem consolidate example/lib/fib.rb"
end
